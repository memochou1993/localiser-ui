export const Scope = Object.freeze({
  System: 'system',
  Project: 'project',
});

export const Role = Object.freeze({
  Admin: 100,
  Developer: 90,
  ProjectOwner: 60,
  ProjectMaintainer: 50,
  ProjectGuest: 40,
});

export default null;
