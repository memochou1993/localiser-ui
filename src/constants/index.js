export const Scope = Object.freeze({
  System: 'system',
  Project: 'project',
});

export const Role = Object.freeze({
  Admin: 100,
  User: 90,
  ProjectOwner: 40,
  ProjectMaintainer: 30,
  ProjectReporter: 20,
  ProjectGuest: 10,
});

export default null;
